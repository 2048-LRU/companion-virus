services:
  companion-virus:
    build: .
    container_name: companion-virus
    restart: unless-stopped
    ports:
      - "6080:6080" # noVNC
      - "5900:5900" # VNC
    stdin_open: true
    tty: true
    volumes:
      - ./home/user1:/home/user1
